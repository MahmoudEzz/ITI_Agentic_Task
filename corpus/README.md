# corpus

Synthetic seed documents for the D6 (HR talent screening) domain pack, tracked in `manifest.json` (per-document id, type, page count and counting method, source format, OCR-required flag, adversarial/bias/injection fixture flag).

All content is fictional — synthetic names, companies, and CVs generated for this assessment. No real personal data. See `docs/AI-USAGE-LOG.md` for how the LLM-assisted generation was done and verified, and `docs/BRD.md` for the full corpus composition rationale.
