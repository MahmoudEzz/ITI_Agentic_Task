# scripts

Standalone entry points invoked via `npm run`, not part of the HTTP surface: `ingest.js` (extract→clean→chunk→embed→index the corpus), `eval.js` (the FR-3 evaluation harness), and any one-off maintenance scripts.
